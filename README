This app will eventually be an open source tool for managing jQuery governance in accordance with its governance rules.

::Redis::

For development, be sure to run redis-server at the default 6379 port.
For testing, be sure to run redis-server at 6380.

You can use:
RAILS_ENV=test rake redis:start
RAILS_ENV=test rake redis:stop
